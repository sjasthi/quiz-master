<?php
/**
 * Database credentials template for Quiz Master.
 *
 * SETUP: Copy this file to "config.php" in the same folder and fill in
 * your own database values. config.php is excluded from git (.gitignore)
 * so real credentials are never committed.
 *
 *   includes/config.sample.php  -> committed to git (this template)
 *   includes/config.php         -> your real credentials (ignored by git)
 */

define('DB_HOST', 'localhost');
define('DB_NAME', 'quiz_master');
define('DB_USER', 'root');
define('DB_PASS', '');
define('DB_CHARSET', 'utf8mb4');
